using UnityEngine;

[System.Serializable]
public class RiffleWeapon
{
    public string name = "yo";

    public int damage = 20;
    public float range = 200f;
    public float fireRate = 200f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
